# 01 — Installera Python

Ingen `.py`-fil. Målet är `python3 --version` (eller `py --version` på Windows) som visar Python 3.x.

Mac: python.org installer eller `brew install python`.  
Windows: python.org, kryssa i “Add python.exe to PATH”.
